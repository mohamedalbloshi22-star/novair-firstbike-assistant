# NSR-1 Test Plan — UI + Packages + Usage + Billing

## الهدف
اختبار النسخة الجديدة قبل دمجها إلى `main` أو نشرها كنسخة حية.

## الحالة الحالية
- ✅ migrations الباقات V1–V4 نُفذت في Supabase.
- ✅ Billing schema V5 نُفذ في Supabase.
- ✅ عميل اختبار بحد 20 رد AI تم إنشاؤه.
- ✅ 70% تم اختباره.
- ✅ 85% تم اختباره.
- ✅ 95% تم اختباره.
- ✅ 100% تم اختباره.
- ✅ الطلب التالي بعد بلوغ الحد تم منعه.
- ✅ اختبار `release` نجح: انخفض الاستخدام من 20 إلى 19 وأصبح المتبقي 1.
- ✅ اختبار rollover للدورة الشهرية نجح: انتقلت الدورة وبدأت الدورة الجديدة بـ `used = 0`.
- ✅ الحد الشهري مربوط داخل `api/chat.js` على فرع الاختبار.
- ✅ آخر نشر Vercel للفرع نجح.
- ⏸️ Stripe الفعلي مؤجل حتى صدور الرخصة التجارية وإنشاء حساب Stripe.

## 1. قاعدة البيانات
تم تشغيل:
1. `supabase/nsr1-packages-v1.sql`
2. `supabase/nsr1-packages-v2.sql`
3. `supabase/nsr1-packages-v3.sql`
4. `supabase/nsr1-packages-v4-security.sql`
5. `supabase/nsr1-billing-v5.sql`

تأكد من وجود:
- `nsr_plans`
- `nsr_client_subscriptions`
- `nsr_usage_counters`
- `nsr_usage_alerts`
- `nsr_subscription_events`
- `nsr_admin_usage_overview`
- `nsr_billing_events`

## 2. اختبار الباقات — مكتمل للحد الأساسي
عميل الاختبار: حد مخصص مؤقت = 20 رد AI.

النتائج:
- ✅ الرد 14 وصل 70%
- ✅ الرد 17 وصل 85%
- ✅ الرد 19 وصل 95%
- ✅ الرد 20 وصل CAP_REACHED
- ✅ الطلب 21 تم منعه ولم يزد العداد عن 20
- ✅ `nsr_release_ai_response` أعاد الحجز بنجاح: `used = 19` و`remaining = 1`

## 3. اختبار فشل AI — جزء release الأساسي ناجح، الاختبار المتكامل مطلوب لاحقًا
تم التحقق من أن دالة `release` نفسها تعيد حجزًا واحدًا بشكل صحيح.

الاختبار المتكامل المطلوب لاحقًا:
- يتم حجز رد قبل استدعاء AI.
- إذا فشل استدعاء AI قبل اكتمال الرد، يتم `release` تلقائيًا من `api/chat.js`.
- لا ينقص رصيد العميل بسبب الرد الفاشل.

ملاحظة: إذا تم إنشاء الرد فعليًا وإرساله للمستخدم ثم فشل فقط حفظ نسخة المساعد في قاعدة البيانات، فيُعامل الرد على أنه مستخدم فعليًا ولا يلزم إرجاع الحجز.

## 4. اختبار الدورة الشهرية — ناجح
على عميل الاختبار `nsr-test-20` تم جعل الدورة مستحقة ثم استدعاء `nsr_current_usage`.

النتيجة:
- ✅ تحركت الدورة تلقائيًا.
- ✅ بدأ عداد الدورة الجديدة من `used = 0`.
- ⏳ التحقق من بقاء سجل الدورة السابقة محفوظًا سيتم بفحص read-only قبل التنظيف النهائي.

## 5. واجهة العميل — مطلوب
اختبر `dashboard-light.html`:
- اسم الباقة
- الحد الشهري
- المستخدم
- المتبقي
- النسبة
- بداية ونهاية الدورة
- تنبيهات 70/85/95/100
- إحصائيات التشغيل القديمة لم تتأثر

اختبر `client-portal.html`:
- زر إدارة الباقة والدفع ظاهر ويقود إلى `billing.html`.
- لا تظهر أي بيانات داخلية أو تكلفة AI للعميل.

## 6. لوحة الإدارة — مطلوب
اختبر `admin-light.html`:
- Active Clients
- MRR
- Allocated Responses
- Used Responses
- Portfolio Utilization
- 70 / 85 / 95 / Capped
- تكلفة AI
- متوسط تكلفة الرد
- الاشتراك الشهري
- Gross Contribution
- AI Cost %

## 7. إدارة العملاء — مطلوب
اختبر `clients-light.html`:
- لا يمكن إنشاء عميل بدون باقة
- تظهر تفاصيل الباقة قبل الحفظ
- بعد الحفظ يظهر العميل مع الباقة والحد والاستخدام والمتبقي والنسبة وتاريخ التجديد والحالة
- يمكن تعديل الباقة من إدارة NOVAIRE

## 8. شرط مهم قبل الدمج — العملاء التجاريون فقط
قبل دمج الفرع إلى `main`:
- يجب التأكد أن كل عميل تجاري نشط لديه سجل صالح في `public.nsr_client_subscriptions`.
- عملاء الاختبار/الديمو لا يُحسبون اشتراكات مدفوعة.
- أي عميل تجاري نشط بدون باقة سيُمنع من استخدام AI بعد تفعيل الكود الجديد.

عملاء الاختبار المستثنون حاليًا:
- `demo-clinic`
- `first-bike`
- `novaire-test-center`
- `nsr-test-20`

## 9. الواجهة العامة — مطلوب
اختبر `index-light.html`:
- عربي / إنجليزي
- إرسال رسالة
- Streaming
- Quick Questions
- Human Handoff
- Callback
- Mobile layout
- عند بلوغ الحد الشهري تظهر رسالة مناسبة ولا يحدث خطأ 500

## 10. Billing / Stripe — البنية جاهزة، التفعيل مؤجل
- ✅ قاعدة بيانات Billing V5 جاهزة.
- ✅ صفحة `billing.html` جاهزة.
- ✅ Checkout / Customer Portal / Webhooks جاهزة في الكود.
- ⏸️ إنشاء حساب Stripe والأسعار والمفاتيح مؤجل حتى صدور الرخصة.

بعد إصدار الرخصة:
1. إنشاء حساب Stripe.
2. إنشاء أسعار Essential / Pro / Enterprise في Stripe.
3. إضافة `STRIPE_SECRET_KEY` و`STRIPE_WEBHOOK_SECRET` و`APP_URL` في Vercel.
4. ربط Price IDs مع `nsr_plans`.
5. اختبار Checkout في Test Mode.
6. اختبار Customer Portal وتغيير الباقة والتجديد التلقائي.
7. التأكد من وصول Webhooks مرة واحدة فقط إلى `nsr_billing_events`.

## 11. التنظيف بعد الاختبارات
بعد نجاح الاختبارات:
- حذف عميل الاختبار `nsr-test-20` إذا لم يعد مطلوبًا.
- التأكد من عدم بقاء حدود اختبار مخصصة على أي عميل حقيقي.

## 12. شرط الدمج إلى main
لا يتم الدمج قبل:
- نجاح اختبارات الواجهات.
- نجاح اختبار فشل AI المتكامل أو اعتماد اختبار بديل آمن له.
- التحقق من بقاء سجل الدورة السابقة محفوظًا.
- التأكد من أن كل عميل تجاري نشط لديه باقة.
- عدم وجود أخطاء 500 أو أخطاء SQL أو فقدان بيانات.
- تفعيل واختبار Stripe يمكن أن يبقى مؤجلًا فقط إذا بقيت خاصية الدفع غير مفعلة للإنتاج حتى إصدار الرخصة.
