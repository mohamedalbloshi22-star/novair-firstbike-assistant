const SUPABASE_URL=process.env.SUPABASE_URL;const SUPABASE_KEY=process.env.SUPABASE_SERVICE_ROLE_KEY;const{isAdminSession}=require('./_admin-session');async function sb(path){const r=await fetch(`${SUPABASE_URL}/rest/v1/${path}`,{headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,'Content-Type':'application/json'}});const t=await r.text();if(!r.ok)throw new Error(`Supabase ${r.status}: ${t}`);return t?JSON.parse(t):[]}module.exports=async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});if(!isAdminSession(req))return res.status(401).json({error:'Unauthorized'});try{const plans=await sb('nsr_plans?active=eq.true&select=code,name,setup_fee_aed,monthly_fee_aed,monthly_ai_responses&order=monthly_ai_responses.asc');return res.status(200).json({plans})}catch(e){console.error('PACKAGES API ERROR',e);return res.status(500).json({error:'Unable to load packages'})}};