const SUPABASE_URL=process.env.SUPABASE_URL;
const SUPABASE_KEY=process.env.SUPABASE_SERVICE_ROLE_KEY;
const {isAdminSession}=require('./_admin-session');
async function sb(path,options={}){const r=await fetch(`${SUPABASE_URL}/rest/v1/${path}`,{method:options.method||'GET',headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,'Content-Type':'application/json',...(options.prefer?{Prefer:options.prefer}:{})},body:options.body?JSON.stringify(options.body):undefined});const t=await r.text();if(!r.ok)throw new Error(`Supabase ${r.status}: ${t}`);return t?JSON.parse(t):null;}
function clean(v){return String(v||'').trim();}
function date(v){const s=clean(v);return /^\d{4}-\d{2}-\d{2}$/.test(s)?s:'';}
module.exports=async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(!SUPABASE_URL||!SUPABASE_KEY)return res.status(500).json({error:'Server configuration error'});if(!isAdminSession(req))return res.status(401).json({error:'Unauthorized'});try{const clientId=clean(req.body?.client_id);if(!clientId)return res.status(400).json({error:'client_id is required'});const rows=await sb(`clients?id=eq.${encodeURIComponent(clientId)}&select=id,config&limit=1`);const client=Array.isArray(rows)?rows[0]:null;if(!client)return res.status(404).json({error:'Client not found'});const config=client.config&&typeof client.config==='object'?client.config:{};const months=Math.max(0,Math.min(120,Number(req.body?.contract_term_months||0)))||null;const patch={...config,contract_start_date:date(req.body?.contract_start_date),contract_end_date:date(req.body?.contract_end_date),contract_term_months:months,client_legal_name:clean(req.body?.client_legal_name),client_license_no:clean(req.body?.client_license_no),client_signatory_name:clean(req.body?.client_signatory_name),client_signatory_title:clean(req.body?.client_signatory_title),contract_status:clean(req.body?.contract_status)||'draft'};await sb(`clients?id=eq.${encodeURIComponent(clientId)}`,{method:'PATCH',prefer:'return=minimal',body:{config:patch}});return res.status(200).json({success:true,contract:{contract_start_date:patch.contract_start_date,contract_end_date:patch.contract_end_date,contract_term_months:patch.contract_term_months,contract_status:patch.contract_status}});}catch(error){console.error('CLIENT CONTRACT ERROR:',error);return res.status(500).json({error:'Unable to save contract data'});}};
